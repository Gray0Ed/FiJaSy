import unittest
import thread
import time
import socket
import threading
from time import sleep
from server import Server

def send_list(sock, ch_list):
    sock.sendall(sock)

if __name__ == '__main__':
      unittest.main()
